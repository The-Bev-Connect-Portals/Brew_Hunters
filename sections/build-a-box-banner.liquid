{%- liquid
  assign scheme = section.settings.color_scheme | default: 'ember'
-%}
<section class="band band--tight-top scheme scheme--{{ scheme }}" id="{{ section.settings.anchor | default: 'mix' }}">
  <div class="wrap mix">
    <div class="mix__copy armed">
      {%- if section.settings.eyebrow != blank -%}
        <p class="eyebrow">{{ section.settings.eyebrow }}</p>
      {%- endif -%}
      <h2>{{ section.settings.heading }}</h2>
      {%- if section.settings.text != blank -%}<p>{{ section.settings.text }}</p>{%- endif -%}
      {%- if section.settings.cta_label != blank -%}
        <div class="hero__actions">
          <a class="cta cta--gold" href="{{ section.settings.cta_url | default: routes.root_url }}">{{ section.settings.cta_label }}</a>
        </div>
      {%- endif -%}
    </div>

    <div class="mix__art armed">
      {%- if section.settings.image -%}
        <img src="{{ section.settings.image | image_url: width: 840 }}"
             alt="{{ section.settings.image.alt | default: section.settings.heading | escape }}"
             width="560" height="510" loading="lazy">
      {%- elsif section.settings.fallback_asset != blank -%}
        <img src="{{ section.settings.fallback_asset | asset_url }}"
             alt="{{ section.settings.fallback_alt | escape }}"
             width="560" height="510" loading="lazy">
      {%- else -%}
        {{ 'product-2' | placeholder_svg_tag }}
      {%- endif -%}

      {%- if section.settings.sticker_text != blank -%}
        {% render 'arc-sticker',
          text: section.settings.sticker_text,
          ring: 'var(--spruce)',
          ink: 'var(--gold)',
          modifier: 'sticker--mix',
          uid: section.id %}
      {%- endif -%}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Build a box banner",
  "tag": "div",
  "settings": [
    {
      "type": "select",
      "id": "color_scheme",
      "label": "Color scheme",
      "options": [
        { "value": "ember", "label": "Ember" },
        { "value": "surf", "label": "Surf" },
        { "value": "spruce", "label": "Spruce" },
        { "value": "gold", "label": "Gold" }
      ],
      "default": "ember"
    },
    { "type": "text", "id": "anchor", "label": "Anchor id", "default": "mix" },
    { "type": "text", "id": "eyebrow", "label": "Eyebrow", "default": "Build a box" },
    { "type": "textarea", "id": "heading", "label": "Heading", "default": "Your box. Your beers." },
    { "type": "textarea", "id": "text", "label": "Text", "default": "Pick 6 or 12 singles from across the roster and we'll pack them together in one box. Each can is priced individually." },
    { "type": "image_picker", "id": "image", "label": "Image" },
    { "type": "text", "id": "fallback_asset", "label": "Fallback theme asset", "info": "Filename in assets/. Used only when no image is chosen above.", "default": "mixed-box.webp" },
    { "type": "text", "id": "fallback_alt", "label": "Fallback image alt text", "default": "Mixed twelve pack of Central Coast craft beer" },
    { "type": "text", "id": "cta_label", "label": "Button label", "default": "Start building" },
    { "type": "url", "id": "cta_url", "label": "Button link" },
    { "type": "text", "id": "sticker_text", "label": "Sticker text", "default": "Mix it · Match it · Ship it ·" }
  ],
  "presets": [{ "name": "Build a box banner" }]
}
{% endschema %}
